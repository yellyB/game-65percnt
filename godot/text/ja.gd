extends RefCounted
## 日本語 번역 스텁. 값을 채우면 반영됨. 비어있는 키는 한국어(ko)로 폴백.
## 번역 시작: text/ko.gd 의 data() { ... } 내용을 여기로 복사한 뒤 값만 일본어로 바꾸세요.
## (키는 절대 바꾸지 말 것)

static func data() -> Dictionary:
	return {
		# 例) "title_name": "愛を測ります",
	}
